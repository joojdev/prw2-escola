info:
  name: Buscar alunos
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/alunos"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
