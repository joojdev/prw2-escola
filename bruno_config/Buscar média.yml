info:
  name: Buscar média
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/alunos/medias"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
