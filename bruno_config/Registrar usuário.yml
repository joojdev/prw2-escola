info:
  name: Registrar usuário
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/register"
  body:
    type: json
    data: |-
      {
        "username": "joao.vitor",
        "password": "abcd1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
