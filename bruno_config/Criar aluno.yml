info:
  name: Criar aluno
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/alunos"
  body:
    type: json
    data: |-
      {
        "id": 67,
        "nome": "Pedro",
        "ra": "11111",
        "nota1": 8.5,
        "nota2": 9.5
      }
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
