info:
  name: Deletar aluno
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/alunos/1"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
